import React, { Component } from 'react';
import './Event.csså';

export default class Event extends Component {
    render() {
        return (
            <div>
            </div>
        );
    }
}