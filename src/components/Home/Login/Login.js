import React from 'react';
import './Login.css';

const Login = (props) => {
    const { username, password } = props;
    return (
        <div>
            <div>
            
            </div>
        </div>
    );
};

export default Login;